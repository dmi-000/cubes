# Build 256-bit ℤ[√d] engine

> **RETROACTIVE SPECIFICATION — recovered, not contemporaneous.**
> This is the prompt as it was sent, extracted from the session
> transcript by `extract_specs.py` on demand. It records what was
> ASKED. It does not certify that the delivered code satisfies it:
> no diff against this text was performed at the time, because this
> text was not on disk at the time. See
> [FAILURE_MODES 19](../../FAILURE_MODES.md#19-the-specification-is-the-one-artifact-not-kept).

| | |
|---|---|
| Delegated | 2026-08-02T21:02:28 |
| Agent type | general-purpose |
| Model override | sonnet |
| Session | `c4196554-d37e-44f9-8da5-5d7210e1f156` |
| Tool-use id | `toolu_01TBuhXjWr6Y5vkrj1NYfnoE` |
| Files named | `cube_regions_q2.cpp`, `cube_regions_q2w.cpp`, `wide_engine_report.md` |
| Present in repo | `cube_regions_q2.cpp`, `cube_regions_q2w.cpp`, `wide_engine_report.md` |
| Cited in LEDGER/RESULTS | `cube_regions_q2.cpp`, `cube_regions_q2w.cpp`, `wide_engine_report.md` |

## Prompt as sent

```text
Working directory: /Users/dmi/carroll

Read /Users/dmi/carroll/WIDE_ENGINE_SPEC.md in full, then read /Users/dmi/carroll/cube_regions_q2.cpp (1106 lines — read the whole thing, especially the ~100-line header comment deriving the overflow budget, and FieldElem, mulU128, cmpU256, pipelineBound, validateBudget).

Implement the spec: `cube_regions_q2w.cpp`, a copy of the validated engine with its scalar widened from __int128 to a signed 256-bit type, the budget thresholds retraced, and a 512-bit sign predicate. Build it with the same compiler flags the existing binary used (check for a build line in CPP_SPEC.md or a Makefile; otherwise `c++ -O2 -std=c++17 -o cube_regions_q2w cube_regions_q2w.cpp`). Then run every gate G1–G5 and write `wide_engine_report.md`.

Critical constraints:
- Do NOT modify cube_regions_q2.cpp, cube_regions_n, or any .md file except the report you write.
- The widened engine is only worth having if it is provably the same engine. G1 (3000+ configs, both engines, identical bounded AND by_depth) is the gate that establishes that. If any config disagrees, stop and report it with the config — do not adjust the gate.
- Silent truncation on overflow is the exact failure mode this project guards against. Budget violations must throw, not clamp.
- Exact integer arithmetic only; no floating point anywhere in a decision.

Report back with: the G1 comparison count and verdict, G2/G3 results, the G4 trial count, the G5 timing ratio, the new admissible ceilings, and how many of the 284 634 previously-rejected n=6 configs now fit in budget.
```
